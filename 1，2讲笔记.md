1.文件管理注意代码存放的位置
创立独立文件夹
2快捷操作：ctrl＋f5 生成二进制编译并运行
ctrl＋c复制
ctrl＋v粘贴
ctrl不放滑动鼠标滚轮能放大
3.C语言每一行独立运行，不可换行拆分
/n换行
4变量声明在函数外，一切操作在函数内
5.（1）int类型占位符%d引号后输出占位的结果
float类型占位符%f，占位符%与d，f中间
如%9d，显示宽度为9位。%9.2f表示最小栏宽为9精确到小数点后两位。
（2）占位符类型和数量要严格对应

6.define宏定义，前等后
7&改变变量值例………，&a，&b
第二课
1.赋值只有＝左边的数会改变
a＝a＋2等价于a＋＝2
2.赋值：a＝2 b＝3
运行a＝b＋＋从左往右运行所以进行运算
a＝b，b＋＋结果输出a＝3，b＝4
反之a＝＋＋b ，输出a＝4，b＝4
3.关系运算符
结果只有真假，真1假0
6＜7是1
6＞7是0
（1）：＝＝判断是否等于
！＝判断不等于

4.if后只能跟进一条语句

解决：if(num%2==0)
{
    num=num*5;
    printf("这是一个偶数");
}

###### 加入一个花括号并入为一个语句

（）括成整体格式

*5*.if可以单独出现而else不能单独，悬空else就近![image-20260915202055553](C:\Users\30931\AppData\Roaming\Typora\typora-user-images\image-20260915202055553.png)

*6*.switch选择，case情况，符合情况后都会执行，需加入break  case可以合并